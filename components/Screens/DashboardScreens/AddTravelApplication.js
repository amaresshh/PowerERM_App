import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AddTravelApplication = ({ navigation }) => {
  return (
    <View>
      <Text>AddTravelApplication</Text>
    </View>
  );
};

export default AddTravelApplication;

const styles = StyleSheet.create({});
